@model List<Senhas.Models.Entities.Usuario>
@{
    ViewData["Title"] = "Administração de Usuários";
    var guiches = ViewBag.Guiches as List<Senhas.Models.Entities.Guiche>;
}
<body>


<div class="admin-container">

    <h2>Administração de Usuários</h2>

    @if (TempData["Sucesso"] != null)
    {
        <div class="success">@TempData["Sucesso"]</div>
    }

    <div class="admin-table">
        <table>
            <thead>
            <tr>
                <th>Nome</th>
                <th>Email</th>
                <th>Guichês</th>
                <th>Ação</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var usuario in Model)
            {
                <tr>
                    <td>@usuario.Nome @usuario.Sobrenome</td>
                    <td>@usuario.Email</td>
                    <td>
                        @if (usuario.UsuarioGuiches.Any())
                        {
                            @string.Join(", ", usuario.UsuarioGuiches.Select(ug => ug.Guiche.Nome))
                        }
                        else
                        {
                            <span>—</span>
                        }
                    </td>
                    <td>
                        <button class="btn-edit" onclick="openModal('modal-@usuario.Id')">Editar Guichês</button>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>

    <!-- Modais -->
    @foreach (var usuario in Model)
    {
        <div id="modal-@usuario.Id" class="modal">
            <div class="modal-content">
                <span class="close" onclick="closeModal('modal-@usuario.Id')">&times;</span>
                <h3>Editar Guichês - @usuario.Nome</h3>
                <form asp-action="AtualizarGuiches" method="post">
                    <input type="hidden" name="usuarioId" value="@usuario.Id"/>
                    <div class="checkbox-group">
                        @foreach (var guiche in guiches)
                        {
                            var selecionado = usuario.UsuarioGuiches.Any(ug => ug.GuicheId == guiche.Id) ? "checked" : "";
                            <label class="checkbox-label">
                                <input type="checkbox" name="guichesSelecionados" value="@guiche.Id" @selecionado/>
                                @guiche.Nome
                            </label>
                            <br/>
                        }
                    </div>
                    <button type="submit" class="btn-save">Salvar</button>
                </form>
            </div>
        </div>
    }
</div>
</body>
<script>
    function openModal(id) {
        document.getElementById(id).style.display = 'block';
    }

    function closeModal(id) {
        document.getElementById(id).style.display = 'none';
    }

    // Fecha modal clicando fora
    window.onclick = function(event) {
        const modals = document.querySelectorAll('.modal');
        modals.forEach(modal => {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        });
    };
</script>


<style>
    :root {
        --cor-primaria: #000000;
        --cor-secundaria: #F6F6F6;
        --cor-terciaria: #22D4FD;
        --cor-hover: #22D4FD;
        --cor-quarta: #1581BF;
        --fonte-primaria: 'Krona One', sans-serif;
        --fonte-secundaria: 'Montserrat', sans-serif;
    }

    * {
        margin: 0;
        padding: 0;
    }

    body {
        box-sizing: border-box;
        background-color: var(--cor-primaria);
        color: var(--cor-secundaria);
    }

   
    .admin-container {
        font-family: var(--fonte-secundaria);
        padding: 160px 40px 40px; /* topo maior para garantir espaço */
        
        color: var(--cor-secundaria);
        min-height: 100vh;
    }

        .admin-container h2 {
            text-align: center;
            margin: 0 auto 30px; /* centraliza horizontalmente */
            width: 900px; /* mesma largura da tabela */
            max-width: 100%; /* mantém responsivo */
            font-size: 2.5rem;
            color: var(--cor-terciaria);
        }


    /* Tabela de usuários */
    .admin-table {
        max-width: 900px;
        margin: 0 auto;
        overflow-x: auto;
    }

        .admin-table table {
            width: 100%;
            border-collapse: collapse;
            background-color: #111;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.5);
            font-size: 1rem;
        }

        .admin-table th, .admin-table td {
            padding: 14px 18px;
            text-align: left;
            border-bottom: 1px solid #333;
        }

        .admin-table th {
            background-color: var(--cor-terciaria);
            color: #000;
            font-weight: 600;
        }

        .admin-table tbody tr:nth-child(even) {
            background-color: #1a1a1a;
        }

        .admin-table tbody tr:nth-child(odd) {
            background-color: #0f0f0f;
        }

        .admin-table tr:hover {
            background-color: #222;
        }

    /* Botões */
    .btn-edit {
        padding: 8px 16px;
        background: var(--cor-terciaria);
        color: #000;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.25s ease;
    }

        .btn-edit:hover {
            background: #00bfff;
            color: #fff;
        }

    .btn-save {
        width: 100%;
        padding: 12px;
        background: var(--cor-terciaria);
        border: none;
        color: #000;
        font-size: 16px;
        font-weight: 600;
        border-radius: 6px;
        cursor: pointer;
        transition: all 0.25s ease;
    }

        .btn-save:hover {
            background: #00bfff;
            color: #fff;
        }

    /* Mensagens de sucesso */
    .success {
        color: #155724;
        background-color: #d4edda;
        border: 1px solid #c3e6cb;
        padding: 12px;
        margin-bottom: 20px;
        border-radius: 6px;
        max-width: 900px;
        margin-left: auto;
        margin-right: auto;
        text-align: center;
    }

    /* Modais */
    .modal {
        display: none;
        position: fixed;
        z-index: 10;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0,0,0,0.7);
    }

    .modal-content {
        background-color: #111;
        color: var(--cor-secundaria);
        margin: 8% auto;
        padding: 30px 40px;
        border-radius: 12px;
        width: 450px;
        position: relative;
        box-shadow: 0 4px 15px rgba(0,0,0,0.7);
    }

    .close {
        color: #aaa;
        position: absolute;
        top: 12px;
        right: 20px;
        font-size: 28px;
        font-weight: bold;
        cursor: pointer;
        transition: color 0.25s ease;
    }

        .close:hover {
            color: var(--cor-terciaria);
        }

    .checkbox-group {
        margin: 20px 0;
    }

    .checkbox-label {
        display: block;
        font-size: 14px;
        margin-bottom: 5px;
        cursor: pointer;
    }

        .checkbox-label input[type="checkbox"] {
            margin-right: 8px;
            cursor: pointer;
        }

</style>